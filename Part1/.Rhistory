print("pass")
mark = 80
if (mark>= 50)
print("pass")
else:
mark = 65
if mark >= 70:
mark = 65
if mark>= 70:
volume_vector <- function(r) {
volumes <- numeric(3)
for (pw in 1:3) {
volumes[pw] <- volume(r^pw)
print(volume(r^pw))
}
return(volumes)
}
volume_vector(r)
mean(scores)
scores[scores > 80]
length(scores)
scores[2] <- 100
scores
scores <- c(88, 65, 80, 98, 72)
mean(scores)
scores[scores > 80]
length(scores)
scores[2] <- 100
scores
df <- data.frame(
name = c("Ava", "Ben", "Cara"),
score = c(80,90,70)
)
str(df)
head(df)
df$name
df[1:2, ]
df[ , "name"]
df=airquality
head(df)
df$date <- ISOdate(1973, df$Month, df$Day)
plot(df$date, df$Temp, type='l')
plot(df$date, df$Temp, type='o')
library("RColourBrewer")
library("RColorBrewer")
display.brewer.all(colorblindFriendly = TRUE)
colours<-brewer.pal(n = 12, name = "Paired")
plot(df$date,df$Temp,type"l",col=colours[2])
plot(df$date,df$Temp,type"l",col=colors[2])
plot(df$date,df$Temp,type="l",col=colors[2])
plot(df$date,df$Temp,type="l",col=colours[2])
plot(df$date,df$Temp,type='o',ylin=c(1,180),col=colours[8])
plot(df$date,df$Temp,type='o',ylim=c(1,180),col=colours[8])
warnings()
lines(df$date,df$Temp,type='o',col=colours[4])
plot(df$date,df$Temp,type='o',ylim=c(1,180),col=colours[8])
lines(df$date,df$Temp,type='o',col=colours[4])
lines(df$date,df$Temp,type='o',col=colours[10])
lines(df$date,df$Temp,type='o',col=colours[4])
plot(df$date,df$Temp,type'o',ylim=c(1,180),col=colours[8])
plot(df$date,df$Temp,type='o',ylim=c(1,180),col=colours[8])
legend('topright', legend=c('Temperature','Ozone'), col=colours[c(8,4)], pch=1)
plot(df$date,df$Temp,type='o',ylim=c(1,180),col=colours[8])
lines(df$date,df$Temp,type='o',col=colours[4])
legend('topright', legend=c('Temperature','Ozone'), col=colours[c(8,4)], pch=1)
plot(df$date,df$Temp, type='l',lwd=2, xlab='Date', ylab='Maximum daily temperature in Fahrenheit', main='Temperature in New York', col=colours[10])
plot(df$date, df$Temp, type='l',lty=2, xlab='Date', ylab='Maximum daily temperature in Fahrenheit', col=colours[6])
#pch = plot character
plot(df$date, df$Temp, type='o',lty=3, pch=19, xlab='Date', ylab='Maximum daily temperature in Fahrenheit', col=colours[12])
plot(df$date, df$Temp, pch=19, xlab='Average wind in mph', ylab='Maximum daily temperature in Fahrenheit', main = 'Temperature vs Wind in New York' col=colours[2])
plot(df$date, df$Temp, pch=19, xlab='Average wind in mph', ylab='Maximum daily temperature in Fahrenheit', main ='Temperature vs Wind in New York', col=colours[2])
df$season[df$Month>6]<-1
df$season[df$Month <= 6] <- 2
table(df$season)
colours<-brewer.pal(n = 12, name = "Paired")
colours<- colours[c(2,6)]
plot(df$Wind, df$Temp, pch=19, col=colours[df$season], xlab='Average wind in mph'.)
)
plot(df$Wind, df$Temp, pch=19, col=colours[df$season], xlab='Average wind in mph'.ylab='Maximum daily temperature in Fahrenheit', main='Temperature vs Wind in New York')
legend('topright', legend=c('post June','May or June'), col= colours, pch - 19)
#matrix scatter plots
pairs(df[1:4], col=colours[df$season], main='Matrix scatter plot of the air quality dataset', oma=c(3,3,3,15))
par(xpd = TRUE)
legend('bottomright', legend=c('post June','May or June'), col= colours, pch=1)
colours<-brewer.pal(n = 12, name = 'Paired')
hist(df$Temp,breaks=20, col=colors[2])
hist(df$Temp,breaks=20,col=colors[2])
hist(df$Temp,breaks=20,col=colours[2],freq=FALSE)
#Second with add=T to plot on top
hist(df$Temp[df$season==2], breaks=20, xlim=c(55,100), col=colours[6], add=T)
#Add legend
legend("topright", legend=c('Post June','May or June'), col=colours[c(2,6)], pt.cex=2, pch=15)
d1<-density(df$Temp[df$season==1])
d2<-density(df$Temp[df$season==2])
plot(d1,xlim=c(45,100), col=colours[2], lwd=2, xlab='height', main='Distribution of Temperature before and after June')
lines(d2, xlim=c(45,100), col=colours[6], lwd=2)
legend('topright', legend=c('Post June','May or June'), col=colours[c(2,6)], lwd=2, pch=15)
boxplot(Temp ~ Month, data=df, col=colours[c(2,6,10,8,4)], names=c('May','Jun','Jul','Aug','Sep'))
library(vioplot)
install.packages("vioplot")
library(vioplot)
vioplot(Temp ~ Month, data=df,col= colours[c(2,6,10,8,4)],names = c('May','Jun','Jul','Aug','Sep'))
#barplot
categories<-c('51-60','61-70','71-80','81-90','91-100')
summary(df$Temp)
df$tTemp_cat<-cut(df$Temp, seq(50,100,10), labels=categories)
Temp_counts<- table(df$Temp_cat)
Temp_counts
barplot(height=Temp_counts, names=categories, col=colours[c(2,6,10,8,4)])
Temp_counts<-table(df$Month,df$Temp_cat)
Temp_counts
barplot(height=Temp_counts, name=categories, col=colours[c(2,6,10,8,4)], legend = c('May','Jun','Jul','Aug','Sep'))
barplot(height=Temp_counts, name=categories, col=colours[c(2,6,10,8,4)], legend = c('May','Jun','Jul','Aug','Sep'),beside=TRUE)
head(mtcars)
data as.matrix(mtcars)
data <- as.matrix(mtcars)
colblues <- colorRampPalette(brewer.pal(8, 'Blues'))(25)
heatmap(data, Colv=NA, scale='coloumn', xlab='variable', main='heatmap',col=colblues)
heatmap(data, Colv=NA, Rowv=NA, scale='coloumn', xlab='variable', main='heatmap',col=colblues)
colblues <- colorRampPalette(brewer.pal(8, 'Blues'))(25)
heatmap(data, Colv=NA, Rowv=NA, scale='coloumn', xlab='variable', main='heatmap',col=colblues)
pdf(file = './Plot.pdf',   # directory to save the file
width = 4, # width of the plot (in inches) optional
height = 4) # height of the plot (in inches) optional
plot(x = 1:10,
y = 1:10)
abline(v = 0) # Additional low-level plotting commands
text(x = 0, y = 1, labels = 'Random text')
dev.off()
#Block7C
library(ggplot2)
str(diamonds)
head(diamonds)
ggplot(diamonds)
ggplot(diamonds) + aes(x = carat)
ggplot(diamonds, aes(x = carat))
# initialise ggplot data and aesthetics layer and
# create a histogram plot with geom_historgram()
ggplot(diamonds, aes(x=carat)) + geom_histogram()
my_hist
my_hist <- ggplot(diamonds, aes(x = carat)) +
geom_histogram(binwidth = 0.1)
my_hist
my_hist +
labs(title = "Weight of Diamonds", x = "Carat", y = "Count") +
theme_bw()
my_kd <- ggplot(diamonds, aes(x=carat)) +
geom_density() +
labs(title = "Weight of Diamonds", x="Carat", y = "Density")
my_kd
str(diamonds)
my_sc <- ggplot(diamonds) +
geom_point(aes(x=carat, y=price, colour=cut), alpha=0.5) +
labs(title= 'Diamonds are forever...', x='Carat', y='Price')
my_sc
my_sc <- my_sc +
geom_hline(aes(yintercept = 5000)) +
geom_vline(aes(xintercept = 1)) +
theme_bw()
my_sc
ggplot(diamonds) + geom_bar(aes(x=cut, colour = cut, fill - cut))
ggplot(diamonds) + geom_bar(aes(x=cut, colour = cut, fill - cut))
scale_fill_brewer(palette = "Greens") +
scale_colour_brewer(palette = "Greens")
library(dplyr)
ggplot(diamonds %>% filter(cut == "Ideal")) +
geom_point(aes(x = carat, y = price), alpha = 0.5) +
labs(title = 'Diamonds are forever...', x = 'Carat', y = 'Price')
ggplot(diamonds[diamonds$cut=="Ideal",]) +
geom_point(aes(x = carat, y = price), alpha = 0.5) +
labs(title = 'Diamonds are forever...', x = 'Carat', y ='Price')
my_sc + facet_grid(~cut)
my_sc + facet_grid(cols=vars(cut))
my_sc + facet_wrap(~cut)
my_hist + facet_grid(color ~ cut)
my_sc + facet_grid(~ cut) + theme_minimal()
my_sc + facet_grid( ~cut) + theme_dark()
library(ggthemes)
install.packages("ggthemes")
library(ggthemes)
my_sc + theme_wsj()
debugSource("~/Documents/SCHOOL/ST2195 Programming/Block07_Lectures/Block07.r")
debugSource("~/Documents/SCHOOL/ST2195 Programming/Block07_Lectures/Block07.r")
setwd("~/Documents/SCHOOL/ST2195 Programming/Coursework/Part1")
lines(density(final_samples), col = "blue", lwd = 2)
log_f <- function(x) {
return(log(0.5) - abs(x))
} #to ensure we always use log
N <- 10000
s <- 1
x0 <- 0
samples <- numeric(N + 1)
samples[1] <- x0
for (i in 1:N) {
x_prev <- samples[i]
x_star <- rnorm(1, mean = x_prev, sd = s) #rnorm = random normal
log_ratio <- log_f(x_star) - log_f(x_prev)
u <- runif(1, min=0, max=1)
f (log(u) < log_ratio) {
log_f <- function(x) {
return(log(0.5) - abs(x))
} #to ensure we always use log
N <- 10000
s <- 1
x0 <- 0
samples <- numeric(N + 1)
samples[1] <- x0
for (i in 1:N) {
x_prev <- samples[i]
x_star <- rnorm(1, mean = x_prev, sd = s) #rnorm = random normal
log_ratio <- log_f(x_star) - log_f(x_prev)
u <- runif(1, min=0, max=1)
if (log(u) < log_ratio) {
samples[i + 1] <- x_star  # Accept
} else {
samples[i + 1] <- x_prev  # Reject
}
}
final_samples <- samples[-1]
sample_mean <- mean(final_samples)
sample_sd <- sd(final_samples)
#cat = print & sprintf = show returns and round to 4df
cat(sprintf("Sample Mean: %.4f\n", sample_mean))
cat(sprintf("Sample Standard Deviation: %.4f\n", sample_sd))
hist(final_samples, freq = FALSE, breaks = 50,
col = "darkblue", border = "darkgray",
main = "Random Walk Metropolis (N=10000, s=1)",
xlab = "x", ylab = "Density",
xlim = c(-8, 8))
lines(density(final_samples), col = "blue", lwd = 2)
x_range <- seq(-10, 10, length.out = 500)
true_f <- 0.5 * exp(-abs(x_range))
lines(x_range, true_f, col = "red", lty = 2, lwd = 2)
legend("topright", legend = c("Kernel Density Estimate", "Truef(x)"),
col = c("blue", "red"), lty = c(1, 2), lwd = 2)
######################################################
#PART 1B
generate_chain <- function(N, s, x0) {
x <- numeric(N +1)
x[1] <- x0
for (i in 1:N) {
x_prev <- x[i]
x_star <- rnorm(1, mean = x_prev, sd = s)
log_ratio <- ((log(0.5)- abs(x_star)) - (log(0.5) - abs(x_prev))
#recall : f(x) = 1/2exp(-|x|)
u <- runif(1, min = 0, max = 1)
######################################################
#PART 1B
#recall formula f(x) = 1/2exp
generate_chain <- function(N, s, x0) {
x <- numeric(N +1)
x[1] <- x0
for (i in 1:N) {
x_prev <- x[i]
x_star <- rnorm(1, mean = x_prev, sd = s)
log_ratio <- ((log(0.5)- abs(x_star)) - (log(0.5) - abs(x_prev))
u <- runif(1, min = 0, max = 1)
######################################################
#PART 1B
#recall formula f(x) = 1/2exp
generate_chain <- function(N, s, x0) {
x <- numeric(N +1)
x[1] <- x0
for (i in 1:N) {
x_prev <- x[i]
x_star <- rnorm(1, mean = x_prev, sd = s)
log_ratio <- (log(0.5)- abs(x_star)) - (log(0.5) - abs(x_prev))
u <- runif(1, min = 0, max = 1)
if (log(u) < log_ratio) {
x[i + 1] <- x_star
} else {
x[i + 1] <- x_prev
}
}
return(x[-1])
}
calculate_R_est <- function(N, s, J, initial_values) {
chains <- matrix(nrow = N, ncol = J)
for (j in 1:J) {
chains[, j] <- generate_chain(N, s, initial_values[j])
}
M_j <- colMeans(chains)
V_j <- numeric(J)
for (j in 1:J) {
V_j[j] <- sum((chains[, j] - M_j[j])^2) / N
}
W <- mean(V_j)
M <- mean(M_j)
B <- sum((M_j - M)^2) / J
R_est <- sqrt((B + W) / W)
return(R_Est)
}
N <- 2000
s_test <- 0.001
J <- 4
initial_values <- c(-2.0, -1.0, 1.0, 2.0)
#r estimate
calculate_R_est <- function(N, s, J, initial_values) {
chains <- matrix(nrow = N, ncol = J)
for (j in 1:J) {
chains[, j] <- generate_chain(N, s, initial_values[j])
}
M_j <- colMeans(chains)
V_j <- numeric(J)
for (j in 1:J) {
V_j[j] <- sum((chains[, j] - M_j[j])^2) / N
}
W <- mean(V_j)
M <- mean(M_j)
B <- sum((M_j - M)^2) / J
R_est <- sqrt((B + W) / W)
return(R_Est)
}
N <- 2000
s_test <- 0.001
J <- 4
initial_values <- c(-2.0, -1.0, 1.0, 2.0)
R_est_value <- calculate_R_est(N, s_test, J, initial_values)
generate_chain <- function(N, s, x0) {
x <- numeric(N +1)
x[1] <- x0
for (i in 1:N) {
x_prev <- x[i]
x_star <- rnorm(1, mean = x_prev, sd = s)
log_ratio <- (log(0.5)- abs(x_star)) - (log(0.5) - abs(x_prev))
u <- runif(1, min = 0, max = 1)
if (log(u) < log_ratio) {
x[i + 1] <- x_star
} else {
x[i + 1] <- x_prev
}
}
return(x[-1])
}
#r estimate
calculate_R_est <- function(N, s, J, initial_values) {
chains <- matrix(nrow = N, ncol = J)
for (j in 1:J) {
chains[, j] <- generate_chain(N, s, initial_values[j])
}
M_j <- colMeans(chains)
V_j <- numeric(J)
for (j in 1:J) {
V_j[j] <- sum((chains[, j] - M_j[j])^2) / N
}
W <- mean(V_j)
M <- mean(M_j)
B <- sum((M_j - M)^2) / J
R_est <- sqrt((B + W) / W)
return(R_est)
}
N <- 2000
s_test <- 0.001
J <- 4
initial_values <- c(-2.0, -1.0, 1.0, 2.0)
R_est_value <- calculate_R_est(N, s_test, J, initial_values)
s_grid <- seq(0.001, 1.0, length.out = 50)
R_est_results <- numeric(length(s_grid))
#r estimate
calculate_R_est <- function(N, s, J, initial_values) {
chains <- matrix(nrow = N, ncol = J)
for (j in 1:J) {
chains[, j] <- generate_chain(N, s, initial_values[j])
}
M_j <- colMeans(chains)
V_j <- numeric(J)
for (j in 1:J) {
V_j[j] <- sum((chains[, j] - M_j[j])^2) / N
}
W <- mean(V_j)
M <- mean(M_j)
B <- sum((M_j - M)^2) / J
R_est <- sqrt((B + W) / W)
return(R_est)
}
N <- 2000
s_test <- 0.001
J <- 4
initial_values <- c(-2.0, -1.0, 1.0, 2.0)
R_est_value <- calculate_R_est(N, s_test, J, initial_values)
s_grid <- seq(0.001, 1.0, length.out = 50)
R_est_results <- numeric(length(s_grid))
for (i in 1:length(s_grid)) {
R_est_results[i] <- calculate_R_est(N, s_grid[i], J, initial_values)
}
plot(s_grid, r_hat_results, type = "b", pch = 16, col = "purple",
xlab = "Step size (s)", ylab = "R-est Value",
main = sprintf("R-hat Values across different step sizes (s)\n(N=%d, J=%d)", N, J))
#r estimate
calculate_R_est <- function(N, s, J, initial_values) {
chains <- matrix(nrow = N, ncol = J)
for (j in 1:J) {
chains[, j] <- generate_chain(N, s, initial_values[j])
}
M_j <- colMeans(chains)
V_j <- numeric(J)
for (j in 1:J) {
V_j[j] <- sum((chains[, j] - M_j[j])^2) / N
}
W <- mean(V_j)
M <- mean(M_j)
B <- sum((M_j - M)^2) / J
R_est <- sqrt((B + W) / W)
return(R_est)
}
N <- 2000
s_test <- 0.001
J <- 4
initial_values <- c(-2.0, -1.0, 1.0, 2.0)
R_est_value <- calculate_R_est(N, s_test, J, initial_values)
s_grid <- seq(0.001, 1.0, length.out = 50)
R_est_results <- numeric(length(s_grid))
for (i in 1:length(s_grid)) {
R_est_results[i] <- calculate_R_est(N, s_grid[i], J, initial_values)
}
plot(s_grid, r_est_results, type = "b", pch = 16, col = "purple",
xlab = "Step size (s)", ylab = "R-est Value",
main = sprintf("R-est Values across different step sizes (s)\n(N=%d, J=%d)", N, J))
#r estimate
calculate_R_est <- function(N, s, J, initial_values) {
chains <- matrix(nrow = N, ncol = J)
for (j in 1:J) {
chains[, j] <- generate_chain(N, s, initial_values[j])
}
M_j <- colMeans(chains)
V_j <- numeric(J)
for (j in 1:J) {
V_j[j] <- sum((chains[, j] - M_j[j])^2) / N
}
W <- mean(V_j)
M <- mean(M_j)
B <- sum((M_j - M)^2) / J
R_est <- sqrt((B + W) / W)
return(R_est)
}
N <- 2000
s_test <- 0.001
J <- 4
initial_values <- c(-2.0, -1.0, 1.0, 2.0)
R_est_value <- calculate_R_est(N, s_test, J, initial_values)
s_grid <- seq(0.001, 1.0, length.out = 50)
R_est_results <- numeric(length(s_grid))
for (i in 1:length(s_grid)) {
R_est_results[i] <- calculate_R_est(N, s_grid[i], J, initial_values)
}
plot(s_grid, R_est_results, type = "b", pch = 16, col = "purple",
xlab = "Step size (s)", ylab = "R-est Value",
main = sprintf("R-est Values across different step sizes (s)\n(N=%d, J=%d)", N, J))
#r estimate
calculate_R_est <- function(N, s, J, initial_values) {
chains <- matrix(nrow = N, ncol = J)
for (j in 1:J) {
chains[, j] <- generate_chain(N, s, initial_values[j])
}
M_j <- colMeans(chains)
V_j <- numeric(J)
for (j in 1:J) {
V_j[j] <- sum((chains[, j] - M_j[j])^2) / N
}
W <- mean(V_j)
M <- mean(M_j)
B <- sum((M_j - M)^2) / J
R_est <- sqrt((B + W) / W)
return(R_est)
}
N <- 2000
s_test <- 0.001
J <- 4
initial_values <- c(-2.0, -1.0, 1.0, 2.0)
R_est_value <- calculate_R_est(N, s_test, J, initial_values)
s_grid <- seq(0.001, 1.0, length.out = 50)
R_est_results <- numeric(length(s_grid))
for (i in 1:length(s_grid)) {
R_est_results[i] <- calculate_R_est(N, s_grid[i], J, initial_values)
}
plot(s_grid, R_est_results, type = "b", pch = 16, col = "purple",
xlab = "Step size (s)", ylab = "R-est Value",
main = sprintf("R-est Values across different step sizes (s)\n(N=%d, J=%d)", N, J))
abline(h = 1.05, col = "red", lty = 2, lwd = 2)
#r estimate
calculate_R_est <- function(N, s, J, initial_values) {
chains <- matrix(nrow = N, ncol = J)
for (j in 1:J) {
chains[, j] <- generate_chain(N, s, initial_values[j])
}
M_j <- colMeans(chains)
V_j <- numeric(J)
for (j in 1:J) {
V_j[j] <- sum((chains[, j] - M_j[j])^2) / N
}
W <- mean(V_j)
M <- mean(M_j)
B <- sum((M_j - M)^2) / J
R_est <- sqrt((B + W) / W)
return(R_est)
}
N <- 2000
s_test <- 0.001
J <- 4
initial_values <- c(-2.0, -1.0, 1.0, 2.0)
R_est_value <- calculate_R_est(N, s_test, J, initial_values)
s_grid <- seq(0.001, 1.0, length.out = 50)
R_est_results <- numeric(length(s_grid))
for (i in 1:length(s_grid)) {
R_est_results[i] <- calculate_R_est(N, s_grid[i], J, initial_values)
}
plot(s_grid, R_est_results, type = "b", pch = 16, col = "purple",
xlab = "Step size (s)", ylab = "R-est Value",
main = sprintf("R-est Values across different step sizes (s)\n(N=%d, J=%d)", N, J))
abline(h = 1.05, col = "red", lty = 2, lwd = 2)
legend("topright", legend = "Convergence Threshold (1.05)",
col = "red", lty = 2, lwd = 2)
